Thank you for your order <%= @order.first_name %>!
For your reference, your receipt is below:

------------------

Order ID: <%= @order.id %>

Your name: <%= @order.first_name %> <%= @order.last_name %>

You've ordered: <% @order.order_items.each do |item| %>
+<%= item.product.title %>
<% end %>
Subtotal: <%= number_to_currency @order.total_price_in_dollars %>
With Tax: <%= number_to_currency @order.total_price_in_dollars * 1.12 %>
+ Shipping: $15.00
Total Price: <%= number_to_currency @order.total_price_in_dollars * 1.12 + 15 %>
<% if @order.couponCode.present? %>
NOTE: You used the coupon: <%= @order.couponCode %> so your total is: <%= number_to_currency @final_amount %>.
Hooray for saving $$! 🤑

------------------
We'll be in touch soon with a shipping update. Until then, see you on the internet!
<% end %>
